# Deployment profile: gpu | cpu
DEPLOY_PROFILE=gpu

# Web ports exposed on the host
FRONTEND_PORT=8080
BACKEND_PORT=8000

# Runtime mode
WEB_PROCESSOR_MODE=pipeline
WEB_PIPELINE_DEBUG=1
WEB_PIPELINE_CONCURRENCY=1
WEB_JOBS_DIR=data/web_jobs

# Models
YOLO_MODEL_PATH=models/yolo26n.pt
OCR_MODE=qwen
MODEL_NAME=qwen2.5vl:3b
MODEL_BASE_URL=http://ollama:11434

# Optional direct URL to a zip with assets. Expected inside zip: yolo26n.pt or models/yolo26n.pt
# ASSET_BUNDLE_URL=https://example.com/gradebook-assets.zip

# Docker names
GRADEBOOK_NETWORK=gradebook_network
OLLAMA_VOLUME=gradebook_ollama_data
OLLAMA_IMAGE_TAG=latest
